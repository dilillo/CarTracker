@model CarTracker.Domain.Commands.InspectBrakesCommand

@{
    ViewData["Title"] = "Inspect Brakes";
}

<h1>Inspect Brakes</h1>

<div class="row">
    <div class="col-md-4">
        <form asp-action="InspectBrakes">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="ID" type="hidden" />
            <div class="form-group">
                <label asp-for="Mileage" class="control-label"></label>
                <input asp-for="Mileage" class="form-control" />
                <span asp-validation-for="Mileage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RemainingPad" class="control-label"></label>
                <input asp-for="RemainingPad" class="form-control" />
                <span asp-validation-for="RemainingPad" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
                @Html.ActionLink("Cancel", "Details", new { id = Model.ID }, new { @class = "btn btn-secondary" })
            </div>
        </form>
    </div>
</div>

@section Scripts {

    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script type="text/javascript">

        $('.custom-file-input').on('change', function () {
            let fileName = $(this).val().split('\\').pop();
            $(this).next('.custom-file-label').addClass("selected").html(fileName);
        });

    </script>
}